<% if session[:vendor_id] %>
	<%= link_to 'Vendor Profile', vendor_path(session[:vendor_id]) %>
<% elsif session[:user_id] %>
	<%= link_to 'Profile Page', user_path(session[:user_id]) %>
<% end %>
<h1>Home Page</h1>

<% if session[:user_id] %>
<p>user id:</p>
<%= session[:user_id] %>
<% end %>
<% if session[:vendor_id] %>
<p>vendor id:</p>
<%= session[:vendor_id] %>
<% end %>
